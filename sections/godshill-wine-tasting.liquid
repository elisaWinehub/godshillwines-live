{% comment %} Wine tasting promo · split editorial row with booking call-to-action {% endcomment %}
{% liquid
  assign show_section = section.settings.section_enabled | default: true
  assign overlay_op = section.settings.section_overlay_opacity | divided_by: 100.0
%}
{% unless show_section %}
  <div {{ section.shopify_attributes }} hidden></div>
{% else %}
  <section
    id="GHTaste-{{ section.id }}"
    class="godshill-wine-tasting gh-wtaste {{ section.settings.custom_css_class | strip }}"
    {{ section.shopify_attributes }}
    style="
      --bg: {{ section.settings.section_background_color }};
      --pt: {{ section.settings.padding_top }}px;
      --pb: {{ section.settings.padding_bottom }}px;
      --cont: {{ section.settings.container_max_width }}px;
      --min-h: {{ section.settings.row_min_height }}px;
      --img-bg: {{ section.settings.image_column_bg }};
      --panel-bg: {{ section.settings.content_column_bg }};
      --eyebrow-c: {{ section.settings.eyebrow_color }};
      --eyebrow-fs: {{ section.settings.eyebrow_size }}px;
      --eyebrow-fw: {{ section.settings.eyebrow_weight }};
      --h-c: {{ section.settings.heading_color }};
      --h-d: {{ section.settings.heading_size_desktop }}px;
      --h-m: {{ section.settings.heading_size_mobile }}px;
      --h-fw: {{ section.settings.heading_weight }};
      --h-ls: {{ section.settings.heading_letter_spacing | divided_by: 100.0 }}em;
      --b-fs: {{ section.settings.body_size }}px;
      --b-c: {{ section.settings.body_color }};
      --b-fw: {{ section.settings.body_weight }};
      --b-ls: {{ section.settings.body_letter_spacing | divided_by: 100.0 }}em;
      --txt: {{ section.settings.content_alignment }};
      --sched-fs: {{ section.settings.schedule_font_size }}px;
      --sched-fw: {{ section.settings.schedule_font_weight }};
      --sched-c: {{ section.settings.schedule_color }};
    "
  >
    {% if section.settings.section_background_image != blank %}
      <div class="gh-wtaste__bg" aria-hidden="true">
        {{
          section.settings.section_background_image
          | image_url: width: 2400
          | image_tag: loading: 'lazy', class: 'gh-wtaste__bg-img', widths: '800,1400,2000'
        }}
        <div
          class="gh-wtaste__bg-tint"
          style="
            opacity: {{ overlay_op }};
            background: {{ section.settings.section_overlay_color }};
          "
        ></div>
      </div>
    {% endif %}

    {% liquid
      assign desktop_media_first = true
      if section.settings.desktop_media_position == 'right'
        assign desktop_media_first = false
      endif

      assign mobile_media_first = true
      if section.settings.mobile_media_position == 'second'
        assign mobile_media_first = false
      endif
    %}

    <div
      class="gh-wtaste__split wine-tasting-grid godshill-wine-tasting__grid"
      data-d-first="{% if desktop_media_first %}media{% else %}content{% endif %}"
      data-m-first="{% if mobile_media_first %}media{% else %}content{% endif %}"
    >
      <div class="gh-wtaste__media wine-tasting-image godshill-wine-tasting__media" style="background: var(--img-bg);">
        {% if section.settings.media_image != blank %}
          {{
            section.settings.media_image
            | image_url: width: 1600
            | image_tag:
              widths: '400,720,1100',
              sizes: '(min-width:990px) 50vw, 100vw',
              class: 'gh-wtaste__photo',
              loading: 'lazy'
          }}
        {% else %}
          <div class="gh-wtaste__photo gh-wtaste__photo--placeholder" role="presentation"></div>
        {% endif %}
      </div>

      <div class="gh-wtaste__panel wine-tasting-content godshill-wine-tasting__content" style="background: var(--panel-bg);">
        <div class="gh-wtaste__panel-inner godshill-wine-tasting__content-inner">
            {% if section.settings.subheading != blank %}
              <p class="gh-wtaste__eyebrow">{{ section.settings.subheading }}</p>
            {% endif %}
            {% if section.settings.heading != blank %}
              <h2 class="gh-wtaste__title">{{ section.settings.heading }}</h2>
            {% endif %}

            {% if section.settings.schedule_richtext != blank %}
              <div class="gh-wtaste__schedule-row">
                {% if section.settings.schedule_icon != blank %}
                  <div class="gh-wtaste__sched-icon-wrap" aria-hidden="true">
                    {{
                      section.settings.schedule_icon
                      | image_url: width: 64
                      | image_tag: loading: 'lazy', widths: '32,48,64', class: 'gh-wtaste__sched-icon', alt: ''
                    }}
                  </div>
                {% endif %}
                <div class="gh-wtaste__schedule rte">{{ section.settings.schedule_richtext }}</div>
              </div>
            {% endif %}

            {% if section.settings.body_richtext != blank %}
              <div class="gh-wtaste__body rte">{{ section.settings.body_richtext }}</div>
            {% endif %}

            {% if section.settings.cta_style != 'secondary' %}
              {% render 'godshill-cta-btn',
                label: section.settings.cta_label,
                url: section.settings.cta_link,
                style: 'primary',
                bg: section.settings.cta_primary_bg,
                fg: section.settings.cta_primary_text,
                border_c: section.settings.cta_primary_border,
                h_bg: section.settings.cta_primary_hover_bg,
                h_fg: section.settings.cta_primary_hover_text,
                h_border: section.settings.cta_primary_hover_border
              %}
            {% else %}
              {% render 'godshill-cta-btn',
                label: section.settings.cta_label,
                url: section.settings.cta_link,
                style: 'secondary',
                bg: section.settings.cta_secondary_bg_fill,
                fg: section.settings.cta_secondary_text,
                border_c: section.settings.cta_secondary_border,
                h_bg: section.settings.cta_secondary_hover_bg,
                h_fg: section.settings.cta_secondary_hover_text,
                h_border: section.settings.cta_secondary_hover_border
              %}
            {% endif %}
          </div>
      </div>
    </div>
  </section>

  <style>
    /* Bypass Horizon/global section wrapper inset — full bleed 50|50 grid */
    #shopify-section-{{ section.id }} {
      padding-inline: 0;
      margin-inline: 0;
      max-width: none;
      width: 100%;
      box-sizing: border-box;
    }

    #GHTaste-{{ section.id }}.godshill-wine-tasting.gh-wtaste {
      position: relative;
      width: 100%;
      max-width: none;
      background: var(--bg);
      padding-block: var(--pt) var(--pb);
      padding-inline: 0;
      margin-inline: 0;
      box-sizing: border-box;
      color: var(--b-c);
      overflow-x: clip;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__bg {
      position: absolute;
      inset: 0;
      pointer-events: none;
      overflow: hidden;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__bg-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__bg-tint {
      position: absolute;
      inset: 0;
      pointer-events: none;
    }
    #GHTaste-{{ section.id }} .wine-tasting-grid {
      width: 100%;
      margin: 0;
      padding: 0;
    }
    #GHTaste-{{ section.id }} .wine-tasting-image {
      margin: 0;
      padding: 0;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__split {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0;
      column-gap: 0;
      row-gap: 0;
      width: 100%;
      margin: 0;
      padding: 0;
      min-height: var(--min-h);
      overflow: clip;
      align-items: stretch;
      border-radius: 0;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__split[data-d-first='content'] .gh-wtaste__panel {
      order: 1;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__split[data-d-first='content'] .gh-wtaste__media {
      order: 2;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__split[data-d-first='media'] .gh-wtaste__media {
      order: 1;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__split[data-d-first='media'] .gh-wtaste__panel {
      order: 2;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__media {
      position: relative;
      display: flex;
      flex-direction: column;
      min-width: 0;
      align-self: stretch;
      margin: 0;
      padding: 0;
      overflow: hidden;
      min-block-size: var(--min-h);
    }
    #GHTaste-{{ section.id }} .gh-wtaste__photo {
      flex: 1 1 auto;
      width: 100%;
      min-height: 0;
      min-block-size: var(--min-h);
      height: 100%;
      object-fit: cover;
      display: block;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__photo--placeholder {
      flex: 1 1 auto;
      min-height: 0;
      align-self: stretch;
      background: linear-gradient(135deg, #efebe4, #cfc5b7);
    }
    #GHTaste-{{ section.id }} .gh-wtaste__panel {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: stretch;
      align-self: stretch;
      flex: 1 1 auto;
      margin: 0;
      padding: clamp(36px, 6vw, 80px);
      box-sizing: border-box;
      text-align: var(--txt, left);
      min-width: 0;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__panel-inner {
      width: 100%;
      max-width: none;
      box-sizing: border-box;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__eyebrow {
      margin: 0 0 12px;
      text-transform: uppercase;
      color: var(--eyebrow-c);
      letter-spacing: 0.08em;
      font-size: var(--eyebrow-fs);
      font-weight: var(--eyebrow-fw, 700);
    }
    #GHTaste-{{ section.id }} .gh-wtaste__schedule-row {
      display: flex;
      gap: 10px;
      align-items: flex-start;
      margin: 0 0 14px;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__sched-icon-wrap {
      flex-shrink: 0;
      margin-top: 2px;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__sched-icon {
      width: 22px;
      height: auto;
      display: block;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__schedule {
      font-size: var(--sched-fs);
      font-weight: var(--sched-fw);
      color: var(--sched-c);
      line-height: 1.4;
      margin: 0;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__schedule p:last-child {
      margin-bottom: 0;
    }
    #GHTaste-{{ section.id }} .gh-wtaste__title {
      margin: 0 0 18px;
      font-family: Georgia, serif;
      color: var(--h-c);
      font-size: clamp(calc(var(--h-m, 38px)), 6vw, var(--h-d, 48px));
      font-weight: var(--h-fw, 400);
      line-height: 1.08;
      letter-spacing: var(--h-ls);
    }
    #GHTaste-{{ section.id }} .gh-wtaste__body {
      margin: 0 0 22px;
      font-family: Arial, Helvetica, sans-serif;
      font-size: var(--b-fs, 15px);
      color: var(--b-c);
      font-weight: var(--b-fw, 400);
      letter-spacing: var(--b-ls);
      line-height: 1.65;
    }
    #GHTaste-{{ section.id }} .gh-cta {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 13px 22px;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      font-size: 12px;
      font-weight: 700;
      text-decoration: none;
      transition: 0.22s ease;
    }
    #GHTaste-{{ section.id }} .gh-cta::after {
      content: '\2192';
    }
    #GHTaste-{{ section.id }} .gh-cta--primary:hover,
    #GHTaste-{{ section.id }} .gh-cta--secondary:hover {
      transform: translateY(-1px);
      background-color: var(--gh-h-bg) !important;
      color: var(--gh-h-fg) !important;
      border-color: var(--gh-h-bd) !important;
    }
    @media screen and (max-width: 989px) {
      #GHTaste-{{ section.id }} .gh-wtaste__split {
        grid-template-columns: 1fr;
        min-height: unset;
      }
      #GHTaste-{{ section.id }} .gh-wtaste__media {
        min-block-size: min(55vw, 420px);
      }
      #GHTaste-{{ section.id }} .gh-wtaste__split[data-m-first='content'] .gh-wtaste__panel {
        order: 1;
      }
      #GHTaste-{{ section.id }} .gh-wtaste__split[data-m-first='content'] .gh-wtaste__media {
        order: 2;
      }
      #GHTaste-{{ section.id }} .gh-wtaste__split[data-m-first='media'] .gh-wtaste__media {
        order: 1;
      }
      #GHTaste-{{ section.id }} .gh-wtaste__split[data-m-first='media'] .gh-wtaste__panel {
        order: 2;
      }
      #GHTaste-{{ section.id }} .gh-wtaste__panel {
        padding-block: clamp(24px, 7vw, 48px);
        padding-inline: clamp(20px, 6vw, 32px);
      }
    }
  </style>
{% endunless %}

{% schema %}
{
  "name": "Godshill Wine Tasting",
  "tag": "section",
  "class": "godshill-wine-tasting",
  "settings": [
    { "type": "checkbox", "id": "section_enabled", "label": "Show section", "default": true },
    { "type": "range", "id": "padding_top", "label": "Top padding", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 0 },
    { "type": "range", "id": "padding_bottom", "label": "Bottom padding", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 0 },
    {
      "type": "range",
      "id": "container_max_width",
      "label": "Content max-width reference",
      "info": "Exposed as CSS variable --cont on #GHTaste-… for custom CSS (section uses edge-to-edge 50|50 columns).",
      "min": 980,
      "max": 1600,
      "step": 10,
      "unit": "px",
      "default": 1180
    },
    {
      "type": "range",
      "id": "row_min_height",
      "label": "Minimum row height (desktop)",
      "min": 320,
      "max": 720,
      "step": 5,
      "unit": "px",
      "default": 445
    },
    { "type": "color", "id": "section_background_color", "label": "Outer section background", "default": "#F7F3E8" },
    { "type": "image_picker", "id": "section_background_image", "label": "Optional full-bleed background image" },
    { "type": "color", "id": "section_overlay_color", "label": "Background tint color", "default": "#000000" },
    { "type": "range", "id": "section_overlay_opacity", "label": "Tint opacity", "min": 0, "max": 100, "step": 5, "unit": "%", "default": 0 },
    { "type": "color", "id": "image_column_bg", "label": "Image column tint", "default": "#f0ebe4" },
    { "type": "color", "id": "content_column_bg", "label": "Content column background", "default": "#F7F3E8" },
    { "type": "image_picker", "id": "media_image", "label": "Featured image" },
    {
      "type": "select",
      "id": "desktop_media_position",
      "label": "Desktop · image placement",
      "options": [
        { "value": "left", "label": "Image left · content right" },
        { "value": "right", "label": "Content left · image right" }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "mobile_media_position",
      "label": "Mobile · stacking order",
      "options": [
        { "value": "first", "label": "Image first · content below" },
        { "value": "second", "label": "Content first · image below" }
      ],
      "default": "first"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Copy alignment inside panel",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ],
      "default": "left"
    },
    { "type": "text", "id": "subheading", "label": "Eyebrow", "default": "Experience" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Wine Tasting" },
    { "type": "image_picker", "id": "schedule_icon", "label": "Optional schedule icon · recommended 96×96" },
    {
      "type": "richtext",
      "id": "schedule_richtext",
      "label": "Highlighted schedule row",
      "default": "<p><strong>▣ Thursday to Sunday | 11am – 5pm</strong></p>"
    },
    {
      "type": "range",
      "id": "schedule_font_size",
      "min": 12,
      "max": 22,
      "step": 1,
      "unit": "px",
      "label": "Schedule row font size",
      "default": 15
    },
    {
      "type": "range",
      "id": "schedule_font_weight",
      "min": 400,
      "max": 800,
      "step": 100,
      "label": "Schedule row weight",
      "default": 700
    },
    { "type": "color", "id": "schedule_color", "label": "Schedule row color", "default": "#25211B" },
    {
      "type": "richtext",
      "id": "body_richtext",
      "label": "Body content",
      "default": "<p><strong>Book by appointment.</strong><br/>Wine tastings during our opening hours are our labour of love. Walk-ins are welcome, however we ask that groups of 6+ book in advance.</p>"
    },
    { "type": "header", "content": "Typography" },
    { "type": "color", "id": "eyebrow_color", "label": "Eyebrow color", "default": "#A67B22" },
    {
      "type": "range",
      "id": "eyebrow_size",
      "label": "Eyebrow font size",
      "min": 10,
      "max": 16,
      "step": 1,
      "unit": "px",
      "default": 12
    },
    {
      "type": "range",
      "id": "eyebrow_weight",
      "label": "Eyebrow weight",
      "min": 400,
      "max": 800,
      "step": 100,
      "default": 700
    },
    { "type": "color", "id": "heading_color", "label": "Heading color", "default": "#25211B" },
    {
      "type": "range",
      "id": "heading_size_desktop",
      "label": "Heading size · desktop",
      "min": 28,
      "max": 64,
      "step": 1,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "heading_size_mobile",
      "label": "Heading size · mobile fallback",
      "min": 24,
      "max": 48,
      "step": 1,
      "unit": "px",
      "default": 36
    },
    {
      "type": "range",
      "id": "heading_weight",
      "label": "Heading weight",
      "min": 400,
      "max": 800,
      "step": 100,
      "default": 500
    },
    {
      "type": "range",
      "id": "heading_letter_spacing",
      "label": "Heading letter-spacing ÷100",
      "min": 0,
      "max": 30,
      "step": 1,
      "default": 0
    },
    { "type": "color", "id": "body_color", "label": "Body color", "default": "#6F6A61" },
    {
      "type": "range",
      "id": "body_size",
      "label": "Body size",
      "min": 12,
      "max": 20,
      "step": 1,
      "unit": "px",
      "default": 15
    },
    {
      "type": "range",
      "id": "body_weight",
      "label": "Body weight",
      "min": 400,
      "max": 700,
      "step": 100,
      "default": 400
    },
    {
      "type": "range",
      "id": "body_letter_spacing",
      "label": "Body letter-spacing ÷100",
      "min": 0,
      "max": 20,
      "step": 1,
      "default": 0
    },

    { "type": "header", "content": "Button & CTA" },
    {
      "type": "select",
      "id": "cta_style",
      "label": "Button style",
      "options": [{ "value": "primary", "label": "Primary" }, { "value": "secondary", "label": "Secondary" }],
      "default": "primary"
    },
    { "type": "text", "id": "cta_label", "label": "Button label", "default": "Book a Tasting" },
    { "type": "url", "id": "cta_link", "label": "Button link" },

    { "type": "color", "id": "cta_primary_bg", "label": "Primary bg", "default": "#A67B22" },
    { "type": "color", "id": "cta_primary_text", "label": "Primary text", "default": "#FFFFFF" },
    { "type": "color", "id": "cta_primary_border", "label": "Primary border", "default": "#A67B22" },
    { "type": "color", "id": "cta_primary_hover_bg", "label": "Primary hover bg", "default": "#846118" },
    { "type": "color", "id": "cta_primary_hover_text", "label": "Primary hover text", "default": "#FFFFFF" },
    { "type": "color", "id": "cta_primary_hover_border", "label": "Primary hover border", "default": "#846118" },

    {
      "type": "paragraph",
      "content": "Secondary/outline button colors"
    },
    { "type": "color", "id": "cta_secondary_bg_fill", "label": "Solid fill accent (outline hover)", "default": "#A67B22" },
    { "type": "color", "id": "cta_secondary_text", "label": "Secondary label", "default": "#A67B22" },
    { "type": "color", "id": "cta_secondary_border", "label": "Secondary border", "default": "#A67B22" },
    {
      "type": "color",
      "id": "cta_secondary_hover_bg",
      "label": "Secondary hover bg",
      "default": "#A67B22"
    },
    { "type": "color", "id": "cta_secondary_hover_text", "label": "Secondary hover text", "default": "#FFFFFF" },
    {
      "type": "color",
      "id": "cta_secondary_hover_border",
      "label": "Secondary hover border",
      "default": "#846118"
    },

    { "type": "text", "id": "custom_css_class", "label": "Additional CSS classes" }
  ],
  "presets": [{ "name": "Godshill Wine Tasting", "category": "God's Hill" }]
}
{% endschema %}
